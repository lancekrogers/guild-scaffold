# Example configuration for {{.vars.name}}
# Copy this file to ~/.{{.vars.name}}.yaml and customize

# Enable debug mode for verbose output
debug: true

# Output format options: json, table, yaml
output: json

# Timeout in seconds for operations
timeout: 600

# Custom options
options:
  parallel: 4
  retry_count: 3
