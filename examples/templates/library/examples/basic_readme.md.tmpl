# Basic Example

This example demonstrates simple usage of {{.vars.name}}.

## Running

```bash
go run main.go
```

## What it does

1. Creates a new client with default options
2. Processes a simple input string
3. Prints the result
