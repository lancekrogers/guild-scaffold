apiVersion: v1
kind: Secret
metadata:
  name: {{ .vars.service_name | lower }}-secrets
type: Opaque
stringData:
  database-url: "postgres://user:password@postgres:5432/{{ .vars.service_name | lower }}?sslmode=require"
  jwt-secret: "change-me-in-production"
  redis-password: ""
