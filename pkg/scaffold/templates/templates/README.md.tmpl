# {{ .vars.campaign_name | title }}

{{ .vars.description | default "A Guild Framework campaign for collaborative AI development." }}

Generated by Guild Scaffold v{{ .vars.scaffold_version | default "0.1.0" }} on {{ date "January 2, 2006" }}.

## Overview

This campaign uses the Guild Framework to coordinate AI agents in {{ .vars.guild_type | default "development" }} workflows. The campaign includes {{ len .vars.guilds }} guild(s) with specialized agent roles for efficient task coordination.

## Campaign Structure

```
{{ .vars.campaign_name }}/
├── .campaign/           # Campaign workspace configuration
│   ├── campaign.yaml    # Main campaign configuration
│   ├── memory.db       # Agent memory database
│   └── archives/       # Long-term memory storage
├── guilds/             # Guild team configurations
{{- range .vars.guilds }}
│   ├── {{ .name }}.yaml
{{- end }}
├── agents/             # Individual agent configurations
{{- range .vars.agent_roles }}
│   ├── {{ .config_file | pathBase }}
{{- end }}
├── commissions/        # Project specifications
├── corpus/             # Knowledge base
└── kanban/             # Task tracking
```

## Quick Start

### Prerequisites

- [Guild CLI](https://github.com/guild-framework/guild-core) installed
- API keys configured for your chosen providers:
{{- range $provider, $config := .vars.providers }}
  - {{ $provider | title }}: Set `{{ $config.api_key_env | default (printf "%s_API_KEY" ($provider | upper)) }}` environment variable
{{- end }}

### Initialize the Campaign

1. **Start the daemon** (if auto-start is disabled):
   ```bash
   guild daemon start
   ```

2. **Verify guild configuration**:
   ```bash
   guild status
   ```

3. **Create your first commission**:
   ```bash
   guild commission create --interactive
   ```

4. **Start working with your guild**:
   ```bash
   guild chat
   ```

## Guild Configuration

### {{ .vars.campaign_name | title }} Guild

{{- if .vars.guild_description }}
{{ .vars.guild_description }}
{{- end }}

**Team Composition:**
{{- range .vars.agent_roles }}
- **{{ .name | title }}** ({{ .type }}) - {{ .description }}
  {{- if .specializations }}
  - Specializations: {{ .specializations | join ", " }}
  {{- end }}
{{- end }}

**Coordination Style:** {{ .vars.coordination_style | default "collaborative" | title }}
**Communication Pattern:** {{ .vars.communication_pattern | default "broadcast" | title }}

## Provider Configuration

This campaign is configured to use the following AI providers:

{{- range $provider, $config := .vars.providers }}
### {{ $provider | title }}
- **Model:** {{ $config.default_model | default "default" }}
- **Cost Magnitude:** {{ $config.cost_magnitude | default "medium" }}
{{- end }}

## Development Workflow

### Task Management

Tasks are managed through the integrated kanban system:
- `kanban/todo/` - New tasks
- `kanban/in-progress/` - Active work
- `kanban/done/` - Completed tasks

### Quality Standards

{{- if .vars.code_review_required }}
- ✅ Code review required for all changes
{{- end }}
{{- if .vars.testing_required }}
- ✅ Comprehensive testing required
{{- end }}
{{- if .vars.documentation_required }}
- ✅ Documentation required for all features
{{- end }}

### Communication Preferences

- **Style:** {{ .vars.communication_style | default "professional" | title }}
- **Verbosity:** {{ .vars.verbosity | default "balanced" | title }}
{{- if .vars.emoji_enabled }}
- **Emojis:** Enabled for enhanced communication
{{- end }}

## Configuration Reference

### Campaign Settings

| Setting | Value | Description |
|---------|-------|-------------|
| Campaign Name | `{{ .vars.campaign_name }}` | Unique campaign identifier |
| Guild Type | `{{ .vars.guild_type | default "development" }}` | Primary guild focus area |
| Max Agents | `{{ .vars.max_agents | default 8 }}` | Maximum agents per guild |
| Parallel Tasks | `{{ .vars.parallel_tasks | default true }}` | Enable parallel task execution |

### Storage Configuration

- **Database:** SQLite at `.campaign/memory.db`
- **Archives:** {{ .vars.archive_compression | default "gzip" }} compression
- **Retention:** {{ .vars.retention_days | default 30 }} days

### Performance Tuning

- **Max Workers:** {{ .vars.max_workers | default 4 }}
- **Request Timeout:** {{ .vars.request_timeout | default "30s" }}
- **Memory Limit:** {{ .vars.memory_limit | default "512MB" }}

## Troubleshooting

### Common Issues

1. **Daemon not starting:**
   ```bash
   guild daemon status
   guild daemon logs
   ```

2. **Agent not responding:**
   ```bash
   guild agent status
   guild memory status
   ```

3. **Provider authentication:**
   - Verify environment variables are set
   - Check API key validity
   - Ensure network connectivity

### Getting Help

- **Documentation:** [Guild Framework Docs](https://guild-framework.dev/docs)
- **Community:** [Discord Server](https://discord.gg/guild-framework)
- **Issues:** [GitHub Issues](https://github.com/guild-framework/guild-core/issues)

## Contributing

To contribute to this campaign:

1. Create a new commission in `commissions/`
2. Update relevant guild configurations
3. Test with your local agents
4. Submit for review

---

*This campaign configuration was generated automatically by Guild Scaffold. Customize as needed for your specific requirements.*